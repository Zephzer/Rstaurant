<form action="/restaurants/{{ restaurant._id }}?_method=DELETE" method="POST" style="display:inline;" id="deleteForm">
    <div class="card">
        <div class="card-body" style="display: flex; align-self: center; flex-direction:column">
            <h5 class="card-title">確定要刪除嗎?</h5>
            <div>
                <button type="submit" class="btn btn-danger">Yes</button>
                <a href="/" class="btn btn-primary">No</a>
            </div>
        </div>
    </div>
</form>